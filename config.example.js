// Copy this to config.js and fill in your values
// This file is for Gateway connection + VAPID push keys only.
// Agent/branding/owner config lives in workspace.config.json (see workspace.config.example.json)
const config = {
  gatewayToken: 'your-gateway-token-here',
  gatewayPort: 18789,
  dashboardPort: 3000
};
